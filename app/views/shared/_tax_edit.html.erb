<%= semantic_form_for @tax, :remote=>true , :html => {:id => 'tc' } do |f| %>
	<%= f.input :state,:as => :radio,:collection => ["VAT","CST"], :name => "tax"  %>
	<%= f.input :rate %>
	<%= f.action :submit,:button_html => { :class => "btn btn-small"} %>
	<% end %>